---
kind: Service
apiVersion: v1beta1
metadata:
  name: jenkins
spec:
  type: LoadBalancer
  selector:
    app: jenkins
  ports:
    - name: http
      protocol: TCP
      # ELB's port
      port: 8080
